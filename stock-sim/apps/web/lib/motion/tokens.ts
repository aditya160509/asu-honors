/**
 * JS mirror of the CSS motion tokens in app/globals.css (--ease-out-expo,
 * --duration-fast/base/slow) — single source of truth for both CSS
 * transitions and GSAP tweens.
 */
export const EASE_OUT_EXPO = "expo.out";
export const EASE_IN_OUT_SMOOTH = "power2.inOut";

export const DURATION_FAST = 0.12;
export const DURATION_BASE = 0.2;
export const DURATION_SLOW = 0.4;
