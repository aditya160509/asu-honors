databases:
  - name: future-lab-db
    databaseName: future_lab
    user: future_lab
    plan: free

services:
  - type: web
    name: future-lab-api
    runtime: python
    plan: free
    rootDir: stock-sim
    # Frontend-only commits deploy on Vercel and must not wake/rebuild the API.
    # Paths are repository-root relative per Render's Blueprint schema.
    buildFilter:
      paths:
        - stock-sim/apps/api/**
        - stock-sim/db/**
        - stock-sim/engine/**
        - stock-sim/pyproject.toml
        - stock-sim/uv.lock
        - stock-sim/render.yaml
    buildCommand: pip install -e . && alembic upgrade head
    startCommand: alembic upgrade head && python db/seeds/run_all.py && uvicorn apps.api.main:app --host 0.0.0.0 --port $PORT --workers 1
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: future-lab-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: COOKIE_SECURE
        value: "true"
      - key: AUTHENTICATION_DISABLED
        value: "false"
      - key: SKIP_EMAIL_VERIFICATION
        value: "true"
      - key: FRONTEND_BASE_URL
        sync: false
      - key: BACKGROUND_WORKER_THREADS
        value: "1"
