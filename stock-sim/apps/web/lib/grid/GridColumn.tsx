// GridColumn is a plain data definition (see types.ts GridColumn<T>), not a React component —
// columns are passed as an array of config objects to VirtualGrid. This file re-exports the
// type for import-site convenience/discoverability alongside the other grid/* files.
export type { GridColumn } from "@/lib/grid/types";
